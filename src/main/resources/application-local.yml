spring:
  h2.console:
    enabled: true
    path: '/console'
  jpa.hibernate:
    ddl-auto: 'validate'
  datasource:
    url: 'jdbc:h2:file:./coach-assistant-db'
management:
  endpoints:
    web:
      exposure:
        include: '*'